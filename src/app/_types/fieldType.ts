export type FieldType = {
    created_at: string | null,
    deletedAt: string | null,
    deletedBy: string | null,
    id: number,
    name: string,
    noOfRecipients: number | null,
    updatedAt: string | null,
};